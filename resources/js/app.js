require('./bootstrap');

// Comentamos la carga de Alpine.js ya que Livewire 3 ya lo incluye
// Esto evita conflictos de múltiples instancias
// import Alpine from 'alpinejs';
// window.Alpine = Alpine;
// Alpine.start();
